import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginTop: 30,
        padding: 12,
    },
    image: {
        // width: 100,
        height: 75,
        aspectRatio: 16/9,
        resizeMode: 'cover',
        borderRadius: 3,
    },
});

export default styles;
