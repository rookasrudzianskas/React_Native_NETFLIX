import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {

    },
    image: {
        width: "100%",
        height: "40%",
        marginTop: 50,
    }
});

export default styles;
