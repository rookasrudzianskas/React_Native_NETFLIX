import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    image: {
        width: 150,
        height: 250,
        resizeMode: "cover",
        borderRadius: 5,
    },
    container: {
        padding: 20,
    },
});

export default styles;
